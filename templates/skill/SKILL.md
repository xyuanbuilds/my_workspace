---
name: { { name } }
description: { { description } }
context: fork
allowed-tools: Bash(node *)
---

# {{name}}

Run the CLI directly via Node.js:

## Execution Pattern

```bash
cd $SKILL_DIR/scripts
node cli.js --help
```
